<div class="error-explanation">
  <h2><%= pluralize(errors.count, "errors") %> prevented this <%= "#{name}" %> from being saved:</h2>
  <ul>
    <% errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
</div>
